version: "3.9"

services:
  ai-service:
    build: .
    container_name: lofo-ai-service
    restart: always
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn app.main:app --host 0.0.0.0 --port 8080